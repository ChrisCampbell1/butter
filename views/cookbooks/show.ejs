<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main>
<h1><%= title %></h1>
<% if (cookbook.recipes) { %>
  <ul>
  <% cookbook.recipes.forEach(recipe => { %>
    <li><a href="/recipes/<%= recipe._id %>"><%= recipe.label %></a></li>
  <% }) %>
  </ul>
<% } else { %>
  <h2>Let's add some recipes!</h2>
<% } %> 
</main>

<%- include('../partials/footer') %>
